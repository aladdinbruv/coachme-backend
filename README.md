# CoachMe - Backend (Express + Mongoose)

Minimal Express backend for the CoachMe project.

Environment variables:

- `MONGO_URI` - connection string for MongoDB

Run locally:

```powershell
cd server
npm install
npm start
```

This is a minimal scaffold. Add auth, validation and secure webhooks before production.
